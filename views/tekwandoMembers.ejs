<!-- views/eurobicsMembers.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Include necessary head elements -->
</head>
<body>
    <h1>Tekwando Members List</h1>
    <ul>
        <% if (members.length > 0) { %>
            <% members.forEach(member => { %>
                <li><%= member.name %> - <%= member.type.name %></li>
            <% }); %>
        <% } else { %>
            <li>No Tekwando Members Found</li>
        <% } %>
    </ul>
    
</body>
</html>
